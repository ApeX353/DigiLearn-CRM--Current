# Local-only notes — never track
CODEBASE-FINDINGS.local.md
*.local.md
/WATCHER.md

# Secrets — never track
.env
.env.*
!.env.example

# Live database dump (real customer data) — never track
db-backup/

# Dependencies & build output
node_modules/
dist/
build/

# Claude Code subagent scratch worktrees — never track
.claude/worktrees/

# OS / editor noise
.DS_Store
Thumbs.db
*.log
/VER-16AUG.md
